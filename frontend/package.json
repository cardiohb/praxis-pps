{
  "name": "pps-frontend",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "@dnd-kit/core": "^6.1.0",
    "@dnd-kit/sortable": "^8.0.0",
    "@tanstack/react-query": "^5.51.11",
    "axios": "^1.7.2",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.25.1",
    "tailwindcss": "^3.4.7"
    
    // Abhängigkeiten für Build-Tools (z.B. Vite oder Create React App)
    // "react-scripts" oder "vite"
  },
  "scripts": {
    "dev": "echo 'Bitte Build-Tool (z.B. vite) konfigurieren' && exit 1",
    "build": "echo 'Bitte Build-Tool (z.B. vite) konfigurieren' && exit 1"
  }
}
